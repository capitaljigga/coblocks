/**
 * WordPress dependencies
 */
const { SVG, Path, G } = wp.components;

/**
 * Block user interface icons
 */
//const icons = {};

var iconList = require('font-awesome-list');
const fontawesome = iconList.all();

export default fontawesome;
